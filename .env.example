# Bee node used by scripts/publish-to-swarm and, later, by the app for feeds.
# A light node is enough. Not needed for Phase 1a (local world).
BEE_URL=http://localhost:1633
# One immutable postage stamp batch id, used for the bundle and for feeds.
BEE_STAMP=
